import { View, Text } from 'react-native'

const SedekahScreen = ({ navigation }) => { 
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Sedekah Screen</Text>
        </View>
    )
}

export default SedekahScreen